"""The web panel: sign-in, tokens, and a read-only view of memory.

Served by ``memgres-server`` when ``MEMGRES_WEB_ENABLED`` is on — the same
image and process as the REST API, no separate build. See docs/WEB.md.
"""
